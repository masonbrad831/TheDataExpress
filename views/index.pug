extends layout

block content
    h1 #{title}
    canvas(id="stateChart" width="400" height="400")
    canvas(id="musicChart" width="400" height="400")
    canvas(id="animalChart" width="400" height="400")
    script(src="https://cdn.jsdelivr.net/npm/chart.js@2.8.0")
    script(src="/homePageCharts.js")